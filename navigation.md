# План: Меню навигации с компасом, метками и пеленгом

> Формат: `[ ]` — не выполнено, `[x]` — выполнено.
> Каждая задача формулируется проверяемо. Подзадачи помечать после успешной компиляции и проверки в симуляторе.

---

## 0. Обзор и архитектура

Текущее поведение: двойное `BACK` на главном экране (`TactixDelegate.onBack()`) вызывает `app.toggleCompass()` — включает/выключает компас.

Новое поведение: двойное `BACK` открывает экран **«Меню навигации»** (`NavMenu`) с тремя пунктами:
1. **Компас вкл/выкл** — toggle + возврат на главный экран.
2. **Установить метку** → подменю работы с одной активной меткой (см. раздел 4).
3. **Показать направление на метку** → выбор метки → возврат на главный, отрисовка стрелки-пеленга и расстояния в метрах.

Новые модули (под `source/`):
- `NavManager.mc` — модель данных и персистентность меток + активного режима пеленга.
- `NavMenuDelegate.mc` — корневое меню навигации.
- `WaypointMenuDelegate.mc` — подменю «Установить метку».
- `WaypointListDelegate.mc` — список меток для удаления и для выбора пеленга.
- `WaypointEditView.mc` + `WaypointEditDelegate.mc` — ручной ввод широты/долготы.
- `MapPickView.mc` + `MapPickDelegate.mc` — выбор точки на офлайн-карте устройства.

Изменения в существующих модулях:
- `TactixDelegate.mc` — заменить `toggleCompass()` на `pushView(NavMenu)`.
- `TactixApp.mc` — добавить состояние `bearingActive`, `bearingTargetIdx`, кэш `Position.Info`; продолжать держать состояние компаса.
- `TactixView.mc` — отрисовка стрелки пеленга по периметру + текста расстояния.
- `manifest_clear.xml` — добавить `iq:uses-permission id="Positioning"` (уже есть) и проверить `Storage`/`Background` при необходимости.
- `resources/strings/strings.xml` — добавить строки локализации (RU/EN через `System.LANGUAGE_RUS`, паттерн как в `AlarmListDelegate`).

---

## 1. Подготовка и инфраструктура

- [ ] **1.1** Проверить, что `Toybox.Position` доступен на всех целевых устройствах (`fenix8solar47mm`, `fenix8solar51mm`, `fenix847mm`, `epix2pro47mm`, `epix2pro42mm`, `fr265s`). Записать результаты в комментарий рядом с импортом.
- [x] **1.2** В `manifest_clear.xml` убедиться, что `Positioning` уже объявлен (да, есть). Дополнительно добавить ничего не требуется.
- [x] **1.3** Создать `source/NavManager.mc` (см. раздел 2).
- [x] **1.4** NavManager сохраняет немедленно в `add()`/`removeAt()` и читает из Storage по требованию — отдельный вызов в `onStart`/`onStop` не нужен.

---

## 2. NavManager — модель и хранилище

Хранение в `Application.Storage` под ключом `nav_waypoints` (массив словарей).

Структура одной метки:
```
{
  "name" => String,   // короткое имя, авто "WP1", "WP2", ...
  "lat"  => Double,   // градусы, -90..90
  "lon"  => Double,   // градусы, -180..180
  "t"    => Long      // unix timestamp создания
}
```

- [x] **2.1** Создать `NavManager` с константой `MAX = 20` (увеличено с 5 по запросу).
- [x] **2.2** Реализовать `NavManager.load() as Array` — читает Storage, возвращает массив (может быть пустой).
- [x] **2.3** Реализовать `NavManager.save(arr as Array) as Void`.
- [x] **2.4** Реализовать `NavManager.add(lat, lon) as Boolean` — добавляет метку с автоименем, возвращает `false` если уже `MAX` меток.
- [x] **2.5** Реализовать `NavManager.removeAt(idx) as Void`.
- [x] **2.6** Реализовать утилиту `NavManager.distanceM(lat1, lon1, lat2, lon2) as Float` — формула гаверсинусов, радиус Земли `6371000.0`.
- [x] **2.7** Реализовать утилиту `NavManager.bearingRad(lat1, lon1, lat2, lon2) as Float` — пеленг в радианах, 0 = север, по часовой.
- [ ] **2.8** Покрыть утилиты на ручной сверке: расстояние Москва→СПб ≈ 635 км, пеленг ≈ 320° (NW).

---

## 3. Перенастройка двойного BACK на главном экране

- [x] **3.1** В `TactixDelegate.onBack()` заменить вызов `app.toggleCompass()` на `pushNavMenu()` (WatchUi.Menu2).
- [x] **3.2** В `TactixDelegate.onSingleBack()` оставить логику выхода с попыткой выключить компас (текущее поведение корректно).
- [x] **3.3** Одиночный BACK: если `bearingActive` — останавливает пеленг; иначе выключает компас / выходит. Двойной BACK открывает NavMenu.

---

## 4. NavMenu — корневое меню навигации

Используем `WatchUi.Menu2` (как в `AlarmListDelegate._pushSubMenu`).

- [x] **4.1** В `source/NavMenuDelegate.mc` создать функцию `pushNavMenu()`:
  - Заголовок: RU «Навигация» / EN «Navigation».
  - Пункт 1: RU «Компас: вкл/выкл» / EN «Compass: on/off», id = `:compass`.
  - Пункт 2: RU «Установить метку» / EN «Set waypoint», id = `:setWp`.
  - Пункт 3: RU «Направление на метку» / EN «Bearing to waypoint», id = `:bearing`.
- [x] **4.2** Реализовать `NavMenuDelegate extends WatchUi.Menu2InputDelegate`.
- [x] **4.3** В `onSelect(item)` для `:compass`:
  - вызвать `app.toggleCompass()`;
  - `WatchUi.popView(SLIDE_DOWN)` до главного экрана.
- [x] **4.4** В `onSelect(item)` для `:setWp`: открывает `WaypointMenu`.
- [x] **4.5** В `onSelect(item)` для `:bearing`: открывает `WaypointListView` в режиме `:pickForBearing`.
- [x] **4.6** В `onBack()` — `WatchUi.popView(SLIDE_DOWN)` (возврат на главный экран без изменения состояния компаса).
- [x] **4.7** Все строки NavMenu локализованы через `System.LANGUAGE_RUS` паттерн.

---

## 5. Подменю «Установить метку»

- [x] **5.1** `pushWaypointMenu()` создана: заголовок RU/EN, пункты `:current` / `:manual` / `:map` (скрыт если нет Toybox.Map) / `:delete`.
- [x] **5.2** `WaypointMenuDelegate extends WatchUi.Menu2InputDelegate`.
- [x] **5.3** Для `:current`: нет GPS → показывает `_NavMsgView` «Нет фикса GPS»; MAX → «Максимум меток»; успех → pop до главного.
- [x] **5.4** Для `:manual`: открывает `WaypointEditView`.
- [x] **5.5** Для `:map`: Plan B — пункт скрыт через `Toybox has :Map`.
- [x] **5.6** Для `:delete`: открывает `WaypointListView :pickForDelete`.

---

## 6. Ручной ввод широты и долготы (WaypointEditView)

Дизайн аналогичен `AlarmEditView` (поля редактирования, листание UP/DOWN, переключение полем SELECT).

- [x] **6.1** `WaypointEditView` рендерит 6 полей: знак/градусы/дробная часть для широты и долготы.
- [x] **6.2** Подписи RU «Широта»/«Долгота», EN «Latitude»/«Longitude».
- [x] **6.3** `WaypointEditDelegate extends NoTouchDelegate`: UP/DOWN ±1, SELECT — след. поле, на поле 5 → `_save()`, BACK — отмена.
- [x] **6.4** Clamp: широта 0..90°, долгота 0..180°, дробная 0..9999.
- [x] **6.5** Масштаб через `dc.getWidth() / 260.0f`.

---

## 7. Список меток (WaypointListView)

Используется в двух режимах: `:pickForDelete` и `:pickForBearing`. Дизайн — упрощённый вариант `AlarmListView`.

- [x] **7.1** `WaypointListView` принимает `mode as Symbol`.
- [x] **7.2** Строки: номер, имя, расстояние от текущей позиции GPS (или `--`). Выделена жёлтым цветом выбранная строка.
- [x] **7.3** Нет меток → «Нет меток»/«No waypoints»; возврат по BACK/UP/DOWN/SELECT.
- [x] **7.4** `WaypointListDelegate`: UP/DOWN навигация; DELETE — удаляет и обновляет; BEARING — `startBearing(idx)` + pop до главного; BACK — pop.

---

## 8. Выбор точки на карте (MapPickView)

> Зависимость: офлайн-карта устройства. ConnectIQ не предоставляет универсального API «показать растровую карту». Реализация в два шага.

- [ ] **8.1** **Исследование:** проверить наличие на целевых устройствах модуля `Toybox.Map` (есть в SDK 4.2+ для некоторых устройств в режиме activity). Если **нет** — переключиться на план Б (см. п. 8.6).
- [ ] **8.2** **План А (Toybox.Map):** Создать `MapPickView extends WatchUi.Map.MapView`. Центрировать на `Position.getInfo().position`. Курсор-крестик в центре экрана.
- [ ] **8.3** **План А:** `MapPickDelegate`:
  - UP/DOWN — изменить zoom;
  - SELECT — короткое нажатие — переключение оси перемещения (lat/lon); длинное — сохранить через `NavManager.add(...)`;
  - BACK — отмена.
- [ ] **8.4** **План А:** прокрутка по lat/lon с помощью таймера (как `mBackTimer` в `TactixDelegate`).
- [ ] **8.5** **План А:** при сохранении — `popView` дважды до главного экрана.
- [ ] **8.6** **План Б (fallback):** если `Toybox.Map` недоступен — пункт «Указать на карте» скрывать на этих устройствах (либо показывать сообщение «Карта недоступна на этом устройстве» и возврат). Реализовать через `Toybox has :Map` проверку при формировании `WaypointMenu`.

---

## 9. Состояние пеленга и отрисовка на главном экране

### 9.1 Модель в TactixApp

- [x] **9.1** Поля `bearingActive`, `bearingTargetIdx`, `bearingLastLat/Lon`, `bearingDistanceM`, `bearingDirectionRad` добавлены в `TactixApp`.
- [x] **9.2** `startBearing(idx)` — сохраняет idx, `bearingActive=true`, включает `Position.LOCATION_CONTINUOUS`.
- [x] **9.3** `stopBearing()` — `bearingActive=false`, `Position.LOCATION_DISABLE`.
- [x] **9.4** `onPositionUpdate(info)` — извлекает координаты, пересчитывает дистанцию и пеленг, вызывает `requestUpdate()`.
- [x] **9.5** Взаимодействие с компасом: если компас активен, угол = `bearingRad - compassHeading`; иначе = `bearingRad` (относительно севера).

### 9.2 Отрисовка в TactixView

- [x] **9.6** `drawBearing(dc, cx, cy)`: выходит если `!bearingActive`, учитывает `compassHeading`.
- [x] **9.7** Стрелка `COLOR_GREEN` через `drawCompassArrow`.
- [x] **9.8** Текст расстояния: `FONT_XTINY`, `COLOR_GREEN`, позиция `rText = rTip - len - 5 - textH/2`, ориентация параллельно периметру через `placeTextColored()`.
- [x] **9.9** При `bearingDistanceM < 0` выводится `"--"` зелёным.

### 9.3 Завершение режима пеленга

- [x] **9.10** Одиночный BACK на главном: если `bearingActive` — вызывает `stopBearing()` и остаётся на экране.
- [x] **9.11** Повторный выбор «Направление на метку» просто перезапускает `startBearing(idx)` с новым индексом.

---

## 10. Локализация

- [x] **10.1** Все строки — инлайн `rus ? "..." : "..."` (единый стиль с alarm-меню).
- [x] **10.2** fr265s деплой выполнен (этап 5); визуальная проверка пункта меню — ручной тест в симуляторе.

---

## 11. Сборка и проверка по устройствам

Для каждого устройства из списка:

- [x] **11.1** `fenix8solar47mm` — сборка OK (этап 4).
- [x] **11.2** `fenix8solar51mm` — сборка OK (этап 4).
- [x] **11.3** `fenix847mm` — сборка OK + деплой в симулятор (этап 4).
- [x] **11.4** `epix2pro47mm` — сборка OK (этап 4).
- [x] **11.5** `epix2pro42mm` — сборка OK (этап 4).
- [x] **11.6** `fr265s` — сборка OK (этап 4).

Сценарии ручной проверки:

- **A. Меню навигации**
  - [ ] двойной BACK → открывается меню;
  - [ ] одиночный BACK → выходит на главный (компас не включается);
  - [ ] BACK из меню → возврат на главный.
- **B. Компас**
  - [ ] выбор «Компас» при выкл → возврат на главный, видны стрелки N/S;
  - [ ] выбор «Компас» при вкл → возврат на главный, стрелок нет.
- **C. Метки**
  - [ ] добавить текущие координаты при отсутствии GPS-фикса → ошибка, метка не создана;
  - [ ] добавить вручную (например, 55.7558 N, 37.6173 E) → видно в списке меток;
  - [ ] удалить метку → исчезает из списка;
  - [ ] попытка добавить 6-ю метку → сообщение «Максимум».
- **D. Пеленг**
  - [ ] выбрать метку через пункт 3 → возврат на главный, стрелка на периметре + расстояние;
  - [ ] расстояние отрисовано **зелёным** шрифтом `FONT_XTINY`, **параллельно периметру** экрана, **в 5 px** от базы стрелки в сторону центра;
  - [ ] при отсутствии GPS-фикса в зоне расстояния выводится `"--"` (тоже зелёным, параллельно периметру);
  - [ ] поворот часов (при включённом компасе) — стрелка и текст расстояния остаются направленными на метку;
  - [ ] выход BACK → пеленг гаснет.

---

## 12. Документация и коммит

- [x] **12.1** `CLAUDE.md` обновлён: добавлена таблица навигационных модулей.
- [ ] **12.2** Добавить в README/store_description упоминание новой функции (опционально, по запросу пользователя).
- [x] **12.3** Коммит `24a94b6` «Меню навигации: компас, метки, пеленг» включает все 12 изменённых файлов.
- [ ] **12.4** Финальный коммит после прохождения сценариев A–D в симуляторе.

---

## 13. Открытые вопросы (требуют решения пользователя до начала кодирования)

- [ ] **13.1** Кнопочный паттерн ручного ввода: использовать `Picker` (стандартный CIQ-виджет) или собственный `View` как `AlarmEditView`? **Рекомендация:** собственный, единый стиль с будильником.
- [ ] **13.2** Сколько хранить меток: 5 (по аналогии с будильниками/таймерами) или больше? **Рекомендация:** 5.
- [ ] **13.3** План А (Toybox.Map) реализовывать сейчас или сразу План Б? Map API сложен и непортируем — может потребоваться отдельная итерация. **Рекомендация:** в первой итерации — План Б (скрыть пункт «На карте» на устройствах без `Toybox.Map`), а План А вынести в отдельную задачу.
- [ ] **13.4** При включённом пеленге показывать **и** стрелки компаса (если активен), или скрыть компас? **Рекомендация:** показывать обе сущности разными цветами.
- [ ] **13.5** Локализация: инлайн-тернарник или `strings.xml`? **Рекомендация:** инлайн (как в alarm-меню) — единый стиль проекта.

---

**Готово к старту реализации после ответов на пункт 13.**
