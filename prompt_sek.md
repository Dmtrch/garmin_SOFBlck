Задача: реализовать секундомер, который отображает время в формате ЧЧ:ММ:СС.

Технические требования к коду:
1. Архитектура: используй паттерн MVC (раздели логику на App, View и Controller/Behavior, если нужны кнопки).
2. Оптимизация экрана (MIP): экран должен обновляться с частотой раз в 1 секунду , чтобы не перегружать процессор и батарею. Для этого используй Toybox.Timer.Timer с шагом 1 с, который вызывает WatchUi.requestUpdate().
3. Точность расчетов: расчет времени должен базироваться НЕ на количестве тиков таймера, а на разнице системного времени через System.getTimer() (миллисекунды с момента старта устройства), чтобы избежать накопления погрешности.
4. Форматирование строк: используй Lang.format() и метод format("%02d") для правильного отображения ведущих нулей в часах, минутах, секундах.
5. Очистка ресурсов: добавь логику остановки таймера в метод onHide() или при остановке секундомера, чтобы приложение не утекало по памяти и не тратило батарею в фоне.
6. Запуск секундомера - в стандартном приложении устройства, остановка или в стандартном приложении устройства или в этом приложении FaceTime двойным нажатием физической кнопки "back". при двойном нажатии секундомер останавливается и устройство активирует стандартное приложение секундомера для дальнейших действий в стандартном приложении секундомера  

Предоставь чистый код файлов:
- SimpleStopwatchApp.mc
- SimpleStopwatchView.mc
- SimpleStopwatchDelegate.mc (для обработки двойного нажатия кнопки "back" физической кнопкой)





Задача: реализовать секундомер, который отображает время в формате ЧЧ:ММ:СС.сс (где «сс» — сотые доли секунды).

Технические требования к коду:
1. Архитектура: используй паттерн MVC (раздели логику на App, View и Controller/Behavior, если нужны кнопки).
2. Оптимизация экрана (MIP): экран должен обновляться с частотой раз в 40 миллисекунд (25 кадров в секунду), чтобы не перегружать процессор и батарею. Для этого используй Toybox.Timer.Timer с шагом 40 мс, который вызывает WatchUi.requestUpdate().
3. Точность расчетов: расчет времени должен базироваться НЕ на количестве тиков таймера, а на разнице системного времени через System.getTimer() (миллисекунды с момента старта устройства), чтобы избежать накопления погрешности.
4. Форматирование строк: используй Lang.format() и метод format("%02d") для правильного отображения ведущих нулей в часах, минутах, секундах и сотых долях.
5. Очистка ресурсов: добавь логику остановки таймера в метод onHide() или при остановке секундомера, чтобы приложение не утекало по памяти и не тратило батарею в фоне.

Предоставь чистый код файлов:
- SimpleStopwatchApp.mc
- SimpleStopwatchView.mc
- SimpleStopwatchDelegate.mc (для обработки нажатия кнопки Старт/Стоп на экране/физической кнопкой)